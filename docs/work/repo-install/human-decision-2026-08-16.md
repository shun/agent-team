# 人間判断: 配布モデル（2026-08-16）

> 状態: 記録（Aira）
> 発言者: 人間
> 原文: 「基本はこのリポジトリでエージェントチームを育てていく。
> 他のリポジトリはこのリポジトリからインストールし、更新もこの
> リポジトリから取得するようにしたい。フィードバックはPR。」

採否の確定は人間発言に限る。Aira の読みは「解釈」と分けて書く。

## 採用したこと

| ID | 内容 |
| --- | --- |
| Q-I-03 | 依頼は続ける。一括セットアップ禁止は、このリポジトリ内の育成に限る。他リポジトリは、このリポジトリを取得元として導入してよい。 |
| Q-B-06 | 更新を計画に含める。更新の取得元もこのリポジトリである。 |
| 配布モデル | このリポジトリが正本。他リポジトリは下流。チームの育てはここで行う。 |
| フィードバック | 下流からのフィードバックは、このリポジトリへの PR とする。 |
| Q-B-02 / 入れ方 | インストーラ（人間が「イメージしているのはA」と指定）。Kai 案B。git 従属ではない。 |

## Aira の解釈（未訂正なら以降こう扱う）

| ID | 解釈 | 根拠 |
| --- | --- | --- |
| Q-I-02 | 導入先の人間が、対象リポジトリで install / update を実行する。このリポジトリのエージェントは、他リポジトリへ書かない。 | 「他のリポジトリがこのリポジトリからインストールし、更新も取得する」は pull。push ではない。REQ-05 とも両立する。 |
| Q-I-01 | 下流へ置くファイルは、このリポジトリの現行定義である。下流で独自の正本にはしない。直しは PR で上流へ戻す。 | 正本はここ、更新はここから、フィードバックは PR。 |
| 非対象 | Mission Room と、このリポジトリ専用の作業履歴は配布しない。 | 下流が欲しいのはチーム定義と起動口であり、デモアプリではない。 |

人間がこの解釈を否定したら、解釈は捨てる。

## 入れ方の採用内容

- 初回: 宣言したファイルだけを、対象リポジトリへコピーする。
- 更新: 同じインストーラを、このリポジトリの新しい参照で再実行する。
- 取得元: この git リポジトリ。パッケージレジストリは使わない。
- 実行者: 導入先の人間。このリポジトリのエージェントは他リポへ書かない。

## まだ決めていないこと

- 配布ファイルの確定一覧
- 既存ファイルとの衝突
- 対象リポジトリの範囲（空のみか、既存か、本番か）
- 導入成功の観測条件

## これで消えた P0

- P0-3: 依頼継続と一括禁止の範囲は、上記 Q-I-03 で解消する。
- P0-1: 解釈どおりなら、書込み主体は導入先の人間。正本緩和は不要。
- P0-2: コピーはする。効力は「上流が正本、下流は追従、変更は PR」。
  下流が独自の実験場正本になることは採用しない。

入れ方と配布一覧は決まった。実行用 `tmp/PLAN.md` は、人間が
PLAN 開始を承認するまで書かない。案は
`docs/work/repo-install/plan-draft.md`。

## 配布一覧（採用 2026-08-16）

人間「これで良いと思う。docs 以下の正本はインストール対象。
team.md、safety.md、workflow.md 以外も。」

入れる:

- `docs/agent/` 配下全部（正本。現在3ファイル）
- `docs/roadmap.md`（正本）
- `docs/decisions/` 配下（正本 ADR）
- ツール別アダプタ（`.codex/`、`.agents/`、`.claude/agents/`）
- `docs/agent/guide.md`（共通ガイドライン。2026-08-16 作成）
- `scripts/run-plan.ts`

入れない:

- `docs/work/`（作業成果物。正本ではない）
- `docs/notes/`（凍結コピー。safety の正本列挙にない）
- `.ai/board/`、`tmp/`、`mission-room/`
- `scripts/run-plan_test.ts`
- ルートのコンテキストファイル（`AGENTS.md`、`CLAUDE.md` など）。
  インストーラは書かない。導入先の人間が、自分のツール用ファイルへ
  「共通コンテキストを読め」と追記する（2026-08-16 人間判断）
