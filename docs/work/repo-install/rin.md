# Rin リスクレビュー — リポジトリ直インストール

- 担当: Rin（凛）
- 作業ID: RI-RIN-1
- 作成: 2026-08-16
- 出力: `docs/work/repo-install/rin.md` のみ
- 状態: 人間判断待ち（P0 あり）。採否・実装・リスク受容はしない

この文書は、指定された計画成果物への反対意見である。設計案の採否、
実装、テスト実行、Q-I-01 / Q-I-02 / Q-I-03 への回答は行わない。

## レビュー範囲と結論

対象は次の6ファイルである。

- `docs/work/repo-install/request.md`
- `docs/work/repo-install/shino.md`
- `docs/work/repo-install/kai.md`
- `docs/work/repo-install/toki.md`
- `docs/work/repo-install/ritsu.md`
- `docs/work/repo-install/hayate.md`

**P0-1 から P0-3 が残るあいだ、案 A から E の採否、実行用
`tmp/PLAN.md` の作成、実装単位・スライスの開始はできない。**
Q-I-01 / Q-I-02 / Q-I-03 は未回答のまま残す。答えを置いて
計画を先へ進める行為自体が、本レビューの停止対象である。

Shino・Kai・Toki・Ritsu・Hayate は、3問を採用しないと明記して
いる。問題はそこではない。問題は、未回答のまま「比較可能」
「単位は書ける」「スライス候補は並べられる」が、依頼の採否と
安全境界の確定に見えることである。

## 重大度順の指摘

### P0-1 — 書込み主体が未決のまま、対象への配置が既定経路になっている

- **対象:** `request.md` 「Aira が観測した事実（仮説ではない）」
  末尾。インストーラの対象リポジトリへの書込みを、「導入先での
  人間操作として扱う必要がある」と書いている。Shino REQ-15 は
  これを規範の提案であり未判断と分類した。REQ-05 は
  `safety.md` 2節のリポジトリ外書き込み禁止で採用済み。
  Kai の5案はいずれも対象への配置または生成を含む。案 B は
  適用器、案 C は従属追加、案 E は新規リポジトリ生成である。
  Ritsu U-APPLY は、エージェント作業として含める契約を現行
  REQ-05 と両立しないとした。Hayate は導入先へ書くスライスを
  除外した。Q-I-02 の答えは置かない。
- **失敗パターン:** Aira または後続が、見出し「仮説ではない」
  を根拠に REQ-15 を採用済みと読む。人間操作というラベルの
  まま、このリポジトリのエージェントが適用器や git 従属を
  実行する。案 B の適用器を「人間が叩く」と書いておき、
  実際の起動をエージェント作業へ混ぜる。Q-I-02 の第2選択肢
  （このリポジトリのエージェント作業）を、安全境界の明示
  変更なしに実装契約へ入れる。
- **緩和策:** Q-I-02 の人間回答と、必要な場合は `safety.md`
  2節の明示変更が揃うまで、対象リポジトリへ書く手段を実装
  対象にも PLAN 対象にもしない。request.md 末尾の一文を事実
  から外し、未判断の規範提案として扱う。エージェントが導入先
  へ書かないことだけを、現行正本として固定する。
- **人間の許容判断:** 対象への書込みを、導入先の人間操作と
  するか、このリポジトリのエージェント作業とするか、今は
  決めずエージェント非書込みだけ固定するか。第2を選ぶなら、
  セキュリティ設定の緩和として正本変更を先に承認するか。
  答えをここには書かない。未回答のまま配置手段の採否へ進む
  ことを許容するか。

### P0-2 — 実験場の正本・起動口を、効力未決のまま配布候補に載せている

- **対象:** `request.md` FACT-02。配布候補に `AGENTS.md`、
  `docs/agent/team.md`、`docs/agent/safety.md`、
  `docs/agent/workflow.md`、`docs/roadmap.md`、
  `.codex/agents/*.toml`、`.claude/agents/rin.md`、
  `.agents/agents.md`、`.agents/workflows/agent-team.md`、
  `.agents/skills/agent-team/SKILL.md`、
  `.agents/skills/team-plan/SKILL.md` を列挙している。
  `AGENTS.md` は「このリポジトリではメイン会話のエージェント
  は Aira」と定め、`jj` 管理を書く。`.agents/skills/agent-team/
  SKILL.md` は7役起動の入口である。Shino REQ-19 は列挙を仮説、
  REQ-27 / Q-I-01 はコピー可否と効力を未確認とした。Kai は
  配布集合を Q-I-01 の差し替えスロットにした。答えは置かない。
- **失敗パターン:** 候補列挙を必須集合として導入先へ置く。
  導入先の既存 `AGENTS.md` や adapter を、この実験場の人格・
  安全境界・Antigravity 起動口で置き換える。コピーを参考と
  称しても、導入先エージェントは `AGENTS.md` を正本として
  読む。拘束するなら、導入先の人間が承認していない VCS 禁止
  と正本変更禁止を押し付ける。コピーしないなら、FACT-02 の
  中心が空になり、残るのはポインタだけである。
- **緩和策:** Q-I-01 の人間回答まで、FACT-02 を必須集合にも
  実装の write 集合にもしない。正本本文、adapter、skill、
  ポインタを別判断にする。導入先に同名のエージェント設定が
  ある場合の扱いを、Q-B-04 とセットで止める。
- **人間の許容判断:** この実験場ルールを他リポジトリへコピー
  してよいか。よければ参考か、導入先でも拘束するか。コピー
  対象を正本本文・adapter・起動 skill のどれに限るか。未回答
  のまま FACT-02 を配布集合の出発点にすることを許容するか。
  答えは書かない。

### P0-3 — 依頼継続の可否が未決のまま、一括配備の計画が前進している

- **対象:** `docs/roadmap.md` 冒頭（正本）は「一括セットアップ
  はしない。最初の一人を最小構成で生み、少しずつ育てる」。
  `request.md` はこれを FACT-05 として引用しつつ、人間依頼
  「いろいろなリポジトリへインストール」を目的に固定した。
  Shino REQ-01 の人間判断は未判断。REQ-29 / Q-I-03 は優先を
  即時確認とし、期限を「計画を続けるかの判断の前」とした。
  Kai は「比較は未回答のまま可能、採否はできない」として
  5案を置いた。Ritsu は Q-I-03 未回答のあいだ U-SRC 以降が
  作業として成立しないと書いた。答えは置かない。
- **失敗パターン:** 7役完成形の多リポジトリ配備を、育成方針
  の例外承認なしに製品化する。Kai の比較表を「計画は揃った」
  と読み、Q-I-03 を後回しにする。案 E のテンプレート初期
  ツリー、または案 A/B の FACT-02 一括複製を、最小構成育成
  の代替にする。依頼を却下すべき場面で、手段比較だけが残る。
- **緩和策:** Q-I-03 の人間回答まで、実行用 PLAN を作らない。
  request.md 自身が「material な不明点が残る場合は作らない」
  と書いており、本問は material である。比較表を採否材料に
  はしてよいが、依頼継続の承認にはしない。
- **人間の許容判断:** 本依頼と roadmap 方針が競合する場合、
  どちらを優先するか。一括禁止の範囲はこのリポジトリ内の
  育成だけか、他リポジトリへの配布も含むか。未回答のまま
  計画を続けることを許容するか。答えは書かない。やらない
  方がよい可能性は、完成形7役を育成ループなしの他リポジトリ
  へ一括配備する依頼そのものにある。

### P1-1 — 衝突方針が仮説のまま、上書きを止められない案が並んでいる

- **対象:** Shino REQ-20 は無断上書きしないを仮説、REQ-26 /
  Q-B-04 は未確認。Kai 案 A は「上書きを技術的には止めない」。
  案 B の既定は衝突時に書かないが、Q-B-04 で上書きへ変えうる。
  FACT-02 のパスは、導入先の `AGENTS.md`、`.agents/`、
  `.codex/`、`.claude/` と同名衝突を起こしうる。Q-B-03 は
  既存・エージェント設定済みを含むか未確認である。
- **失敗パターン:** 既存プロジェクトへ案 A を手順どおり実施
  し、導入先のエージェント契約を黙って置換する。確認欄を
  手順書に置いたことを、上書き防止の証明にする。
- **緩和策:** Q-B-04 と対象クラス（Q-B-03）が固定されるまで、
  既存リポジトリへの配置を実装対象にしない。衝突時の既定は
  書かない、を手段の性質ではなく人間判断として残す。
- **人間の許容判断:** 既存同名ファイルを拒否するか、確認後か、
  マージか、対象外か。既存エージェント設定ありのリポジトリ
  を初回対象に含めるか。

### P1-2 — 「いろいろなリポジトリ」に本番・第三者が入りうる

- **対象:** 人間依頼の「いろいろなリポジトリ」。Shino REQ-21
  はホスト非限定を仮説、REQ-25 / Q-B-03 は空・既存・第三者・
  本番を未確認とした。`safety.md` 1節は本番設定変更、本番
  データ変更、外部サービス契約を、人間の明示承認なしに禁止
  する。Kai 案 E はホスト固有テンプレートにすると REQ-21 と
  食い違うと書いた。
- **失敗パターン:** 対象範囲を固定せず適用器やテンプレートを
  作り、顧客リポジトリや本番へエージェント設定を入れる。
  公開テンプレート化を、配布手段の一部として黙って始める。
- **緩和策:** 初回対象を、人間が所有し本番ではないリポジトリ
  に限るか、含めるなら 1節の承認を先に取る、を人間判断として
  残す。範囲未確定のまま「全リポジトリへ実装可能」と書かない。
- **人間の許容判断:** 対象は空のみか、既存を含むか、第三者・
  本番を含むか。公開テンプレートやホスト固有機能を、外部
  サービス契約として扱うか。

### P1-3 — 「比較可能」が、即時確認の期限と PLAN 停止条件を空にする

- **対象:** Shino Q-I-01 / Q-I-02 の期限は「設計案の比較に
  入る前」。Q-I-03 の期限は「計画を続けるかの判断の前」。
  Kai は3問を未採用の設計軸とし、比較を先に行った。
  `request.md` は material な不明点が残る場合に実行用 PLAN
  を作らないと書いた。Ritsu / Hayate は開始不能とした。
- **失敗パターン:** 5案と比較表があることを「不明点は比較で
  吸収した」と読み、Q-I を後回しにする。Aira が material の
  範囲を再定義し、PLAN 開始条件（Q-B-07）だけを聞く。Hayate
  の HQ-02 / HQ-03 / HQ-04（即時確認）を先に返し、Q-I を
  スライス目的の下位項目へ落とす。
- **緩和策:** Q-I-01 / Q-I-02 / Q-I-03 を、比較表の有無と
  独立した停止条件として扱う。比較済みを依頼継続の承認にも、
  PLAN 作成条件の充足にもしない。HQ 系は Q-I の後、最初の
  実装作業 ID を切る前に限る。
- **人間の許容判断:** 即時確認3問が未回答のまま、統合提案や
  PLAN 条件の議論へ進むことを許すか。許さないなら、Aira は
  3問を先に人間へ戻すか。

### P1-4 — 案 B の適用器は、取得元を差し替え可能な書込み口になる

- **対象:** Kai 案 B。入力に「このリポジトリの URL または
  パス」を置く。成立条件は「取得元をこの git リポジトリに
  限定できる」だが、技術的強制とは書いていない。Cons に
  「適用器自体が高権限の書込み口になる」とある。Toki QR-X1
  は母集団外の備忘である。Ritsu U-FETCH は、エージェント
  作業としてのネットワーク / VCS 取得が safety 2節と今回の
  停止条件に触れると書いた。
- **失敗パターン:** URL 引数のまま実装し、別リポジトリや
  改ざんされた fork を取得元にする。適用器迂回の手コピーと、
  適用器経由の広範囲書込みが並立する。このリポジトリの
  エージェントが取得を代行し、外部送信・認証情報を成果物や
  ログに残す。
- **緩和策:** 案 B を採る前に、取得元の固定方法、許可パス、
  実行主体、ネットワーク / VCS の実施者を人間判断で分ける。
  採否前に適用器を置かない。
- **人間の許容判断:** 新しい実行物を置くか。置くなら、取得元
  の固定を技術的に求めるか、運用宣言で足りるか。

### P1-5 — 案 C の従属は、配布候補外の実行入口まで持ち込みうる

- **対象:** Kai 案 C は「このリポジトリを固定 commit で従属
  させる」。sparse な部分取得は未確認と自ら書いた。案 A は
  Mission Room と `scripts/run-plan.ts` を配布前提にしない。
  `request.md` FACT-03 は `scripts/run-plan.ts` をこの
  リポジトリ内の `tmp/PLAN.md` 実行入口とした。
- **失敗パターン:** 従属がリポジトリ全体を指し、導入先履歴に
  デモアプリと実行入口が残る。導入先で `run-plan.ts` を実行
  し、この実験場の PLAN ランナーを外来ワークフローにする。
  投影なしではツールが根パスを読めず、結局本文をルートへ
  複製して案 A に戻る。
- **緩和策:** 従属を採るなら、持ち込むパス集合を Q-B-01 と
  同時に固定する。全体従属を「直接」の実装形にしない。
- **人間の許容判断:** インストールを git 従属として観測して
  よいか。よければ、全体か、宣言した部分だけか。

### P1-6 — 実装単位の先頭が、案 B を採否前の既定にしている

- **対象:** Ritsu U-SRC の対象は「新規インストーラ成果物」。
  Hayate 候補 B は「このリポジトリ内の dry-run 入口」。
  両者は採用しないと書いた。Kai は案 B を除外していない。
  REQ-03 は PLAN 承認前の実装を禁止する（採用済み）。
- **失敗パターン:** 統合時に U-SRC を最初の実装作業として
  切り、適用器の置き場・言語を「新規だから隔離が狭い」理由
  で先に埋める。dry-run 入口を計画成果物だと読み替え、
  REQ-03 を迂回する。
- **緩和策:** 実装単位の列挙を、設計採否の後に作り直す。
  先頭単位が特定案を暗示するなら、その旨を統合で明示し、
  採否前の作業 ID を切らない。
- **人間の許容判断:** 実装単位の洗い出しを、設計未採択の
  まま残してよいか。残すなら、U-SRC を案 B の前取りとして
  使わないことを条件にするか。

### P1-7 — 配置成功を導入成功に読み替える観測条件が無い

- **対象:** Shino REQ-16 は「配置して使える」を仮説、REQ-30 /
  Q-B-05 は観測条件を未確認とした。Kai は全案で REQ-16 を
  未保証とした。案 D は REQ-16 と食い違いうると書いた。
  Toki QR-08、T-INST-05、T-OPT-X2、G-05 / G-09 は、配置と
  「使える状態」の同一視を Inconclusive にした。
- **失敗パターン:** ファイルが置かれたこと、ポインタが置かれ
  たこと、テンプレートが生成されたことを、インストール完了
  として PLAN やゲートに使う。Toki の analysis-draft を、
  承認者なしのまま受け入れ条件にする。
- **緩和策:** Q-B-05 が返るまで、完了条件と受け入れゲートを
  作らない。案 D を選ぶなら、それを案内手段として REQ-01 の
  充足から外す。Toki 成果の状態
  `analysis-draft / 承認者=なし / ゲート利用不可` を維持する。
- **人間の許容判断:** 計画完了と導入成功を、何を見て判断する
  か。局所に定義が無い参照固定を、インストールに含めるか。

### P1-8 — FACT-02 は起動に必要な集合としても、過剰な集合としても欠けている

- **対象:** FACT-02 に `CLAUDE.md` が無い。`safety.md` 2節は
  `CLAUDE.md` を正本保護対象に含む。Claude adapter は
  `.claude/agents/rin.md` だけである。Codex は6役、
  Antigravity は `.agents/` 一式と7役 skill がある。
  `AGENTS.md` は `jj` 管理を書く。Kai 案 D は局所 adapter
  必須なら成立しない側へ寄ると書いた。
- **失敗パターン:** 列挙をそのまま実装し、Claude 導入先では
  Rin だけが起動する。`CLAUDE.md` だけ導入先に残り、参照先
  の `AGENTS.md` と矛盾する。`jj` 前提と git 禁止を git 運用
  の導入先へコピーし、導入先エージェントが VCS 不能になる。
  逆に Antigravity skill まで置き、導入先で7役が即起動する。
- **緩和策:** 配布集合をツール別の最小集合と、置いてはなら
  ない集合に分け、Q-B-01 と Q-I-01 の回答後にだけ固定する。
  FACT-02 を「少なくともこれ」から必須集合へ昇格しない。
- **人間の許容判断:** 初回配布を、特定ツールの最小 adapter に
  限るか、正本本文を含むか、起動 skill を含むか。

### P2-1 — 削除禁止のまま、戻し手段が計画対象外になっている

- **対象:** REQ-08 は削除と実質削除を禁止（採用済み）。
  Shino Q-D-01 はアンインストールを保留可能とした。Kai 各案
  は後戻りを「置いたファイル次第」「削除は対象外」とした。
- **失敗パターン:** 導入後に配布範囲を誤っても、このチームの
  作業としては撤去できない。導入先が safety をコピーしてい
  れば、導入先エージェントも消せない。
- **緩和策:** 初回対象を新規または空に限るか、戻しを導入先
  人間の VCS 操作として計画外に明示する。
- **人間の許容判断:** アンインストールを今回含めないことを
  許容するか。含めるなら削除禁止との関係を正本で確認するか。

### P2-2 — roadmap / workflow を配ると、導入先にこの実験場の進捗正本が立つ

- **対象:** FACT-02 に `docs/roadmap.md` と
  `docs/agent/workflow.md` がある。roadmap は「進捗の正は
  `docs/work/current-task.md`」、現行メインを Aira と書く。
  workflow は Aira 起点の標準フロー正本である。
- **失敗パターン:** 導入先が、このリポジトリの Stage 履歴と
  進捗ファイルを自分の正本として読む。
- **緩和策:** 育成履歴と手順正本を、配布集合から分ける。
- **人間の許容判断:** 導入先に、この実験場の育成正本を置くか。

### P2-3 — checkout 記載を、全員が独立検証せず共有前提にしている

- **対象:** `request.md` の FACT-01 から FACT-05。Shino /
  Kai / Toki / Ritsu / Hayate は出典確認のみで、リポジトリ
  内容を独立検証していない。
- **失敗パターン:** 列挙漏れや誤引用のまま、配布集合と
  Why（案 A は実行物が無い前提）が固定される。
- **緩和策:** 採否前に、人間または別主体が FACT のパス存在と
  roadmap 引用を独立確認する。確認前に必須集合へしない。
- **人間の許容判断:** 独立検証なしの出典確認で、計画比較を
  続けるか。

### P2-4 — 更新・再導入を後から同一手段へ混ぜうる

- **対象:** Shino REQ-32 / Q-B-06。Kai 案 E は更新を主経路に
  含めない。Toki QR-12。Ritsu は初回単位だけなら記述できる
  とした。
- **失敗パターン:** 初回用の適用器や従属を、更新手段として
  黙って拡張する。
- **緩和策:** 初回のみと明記し、更新は別判断として残す。
- **人間の許容判断:** 今回の計画を初回導入だけに限るか。

## Q-I 未回答のまま進むリスク

Rin は Q-I-01 / Q-I-02 / Q-I-03 に答えない。答えの候補を
選んで「暫定」とすることも、ここではしない。

| 問 | 未回答のまま進むと起きること | 止める地点 |
| --- | --- | --- |
| Q-I-01 | 配布集合と導入先での効力が決まらない。FACT-02 を置いた瞬間に、参考のつもりが拘束になる。置かない選択を、案 D の採択として黙って行う | 配布集合の固定、U-MANIFEST、案の採否 |
| Q-I-02 | 安全境界の適用先が決まらない。人間操作というラベルで REQ-05 を迂回できる。エージェント書込みを選ぶと、正本緩和なしに 2節へ抵触する | 適用器・従属・生成の実装、U-APPLY、PLAN |
| Q-I-03 | 依頼そのものの採否が残る。比較表があることを、一括配備の継続承認と読む。育成方針と両立しない製品化が、手段選定の形で進む | 計画継続、実行用 PLAN、U-SRC 以降 |

3問のどれか一つでも未回答なら、Toki T-PLAN-11 / T-OPT-X1 の
Fail 条件（答えを採用して先へ進む）に当たる作業をしては
ならない。Inconclusive を Pass に読み替えない。

## 人間へ返す最小判断セット

P0 を解消するには、次を個別に回答する必要がある。答えは
ここには書かない。

1. Q-I-02。対象への書込みは誰のどの操作か。現行 REQ-05 を
   維持するか、緩和するか。
2. Q-I-01。実験場ルールを他リポジトリへコピーするか。する
   なら参考か拘束か。対象は本文か adapter か起動口か。
3. Q-I-03。本依頼を続けるか。一括禁止の適用範囲はどこまでか。

P1 の解消には、これに加えて Q-B-03（対象範囲）、Q-B-04
（衝突）、Q-B-01（配布物）、Q-B-05（観測条件）が要る。
これらが残るあいだ、実行用 `tmp/PLAN.md` は作らない。

## 確認した観点

- 安全境界（リポジトリ外書き込み、正本変更、VCS、削除、
  秘密情報、本番操作、セキュリティ緩和）
- 未確認事項を決定事項として扱う楽観（request.md の事実
  見出し、明示情報と採用済みの混同）
- 後戻りできない配備（上書き、従属、テンプレート公開、
  削除禁止）が、可逆な計画のように見えていないか
- 実験場ルールの転用と、導入先エージェント同一性の奪取
- 依頼と roadmap 正本の競合を、比較可能で丸めていないか
- 書込み主体のラベルと、実際の実行者のずれ
- 適用器・git URL・公開テンプレートの供給連鎖
- 配置成功と「チームとして使える」の同一視
- 実装単位・スライス候補による設計の前取り
- テスト分析のゲート転用、独立検証の欠落
- 配布集合の過不足（CLAUDE.md、Rin のみ、7役 skill）
- 対象に本番・第三者を含むか
- 更新・アンインストール・戻し手段の欠落
- やらない方がよい可能性（完成形7役の一括配備そのもの）
- Q-I-01 / Q-I-02 / Q-I-03 未回答のまま進む停止点

## していないこと

- Q-I-01 / Q-I-02 / Q-I-03 への回答、暫定採用、推奨値
- 案 A から E の採否、U-SRC 以降の開始許可
- 実装、テスト実行、VCS、ネットワーク、正本変更
- リスク受容。受容は人間の判断である
