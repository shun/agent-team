# agent-team skill compatibility note

Antigravity CLI 1.1.5 loads this skill from
`.agents/skills/agent-team/SKILL.md`.

This file intentionally has no skill frontmatter. It avoids registering a duplicate
`/agent-team` if a future CLI release also scans flat Markdown files.
